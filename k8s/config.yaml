apiVersion: v1
kind: ConfigMap
metadata:
  name: vectora-config
data:
  VECTORA_MODE: "service"
  LOG_LEVEL: "info"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vectora-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
