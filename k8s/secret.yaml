apiVersion: v1
kind: Secret
metadata:
  name: vectora-secrets
type: Opaque
stringData:
  # Substitua pelos valores reais em seu ambiente através de ferramentas como bitnami-sealed-secrets
  gemini-api-key: "YOUR_GEMINI_API_KEY"
  secret-key: "YOUR_MASTER_SECRET_KEY"
